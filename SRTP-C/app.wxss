/**app.wxss**/
@import "colorui/main.wxss";
@import "colorui/icon.wxss";

.container {
  height: 100%;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: space-between;
  padding: 200rpx 0;
  box-sizing: border-box;
}

/* 设置 nav-bar 的字体样式 */
.bold-text {
  font-weight: bold; /* 粗体 */
  font-size: 19px;
  color: black;
}

.bg-gradual-myself {
  background-image: var(--myself);
  color: darkblue;
}